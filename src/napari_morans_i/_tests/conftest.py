"""Shared pytest configuration for the test suite.

The ``make_napari_viewer`` fixture is provided by napari itself via its
``napari.utils._testsupport`` pytest plugin, which is auto-discovered as
long as ``napari`` and ``pytest-qt`` are installed.
"""
